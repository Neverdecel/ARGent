# Placeholder - Replace with actual sealed secret
# Generate using: kubeseal --cert ~/starnode-core/pub-sealed-secrets.pem --format yaml < ghcr-creds.yaml > sealed-image-pull-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ghcr-creds-prd
  namespace: argent-prd
spec:
  encryptedData:
    .dockerconfigjson: SEALED_VALUE_PLACEHOLDER
  template:
    metadata:
      name: ghcr-creds-prd
      namespace: argent-prd
    type: kubernetes.io/dockerconfigjson
