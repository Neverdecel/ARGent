# Placeholder - Replace with actual sealed secret
# Generate using: kubeseal --cert ~/starnode-core/pub-sealed-secrets.pem --format yaml < secrets.yaml > sealed-secrets.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: argent-secrets
  namespace: argent-prd
spec:
  encryptedData:
    DATABASE_URL: SEALED_VALUE_PLACEHOLDER
    SECRET_KEY: SEALED_VALUE_PLACEHOLDER
    RESEND_API_KEY: SEALED_VALUE_PLACEHOLDER
    GEMINI_API_KEY: SEALED_VALUE_PLACEHOLDER
  template:
    metadata:
      name: argent-secrets
      namespace: argent-prd
    type: Opaque
